# Kotlin control-flow fixture

The executable control-flow corpus lives in `../kotlin-basic/src/main/kotlin/com/acme/Samples.kt`; golden graph assertions are in the Rust and integration test suites.

