# Proposal fixtures

The example uses work-local handles. Resolve these against an actual prepared
work and read the referenced evidence before submitting. Source authority and
acceptance are machine-owned; arbitrary prose remains unassessed until separate
review. Recursive step trees express branches; reference cycles and author-owned
canonical IDs are not part of the format.
