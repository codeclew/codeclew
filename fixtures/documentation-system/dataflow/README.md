# Entity data-flow fixtures

Register the explicit `quantity` entity and `orders` source fixture before saving
`quantity-flow.json`. The definition preserves requested scope and separate human
material; generated nodes and edges belong to the reviewed operation artifact.

The `docsys_t11_*` CLI regressions test module contracts, entity/representation
separation, protected metadata, source/declared/unknown edge authority, mapper
mutation propagation, independent reuse and isolated meaning review. Deterministic
review fixtures establish pipeline behavior, not real model-quality claims.
