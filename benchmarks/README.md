# Benchmarks

Run `scripts/benchmark.sh` for independently timed worker startup/doctor, project inspection, indexing and local CFG on the executable fixture. Corpus-scale SLO validation is a next-stage activity; the small fixture only protects against gross regressions.

