# Local documentation release verdict — 2026-09-13

Codeclew 0.8.0 is ready for a local documentation pilot. T00–T13 are implemented.
The local portion of T17 is implemented: current narrative 1.3 guidance,
compatible legacy readers, explicit unassessed proposal publication, preserved
influence and immutable history, and source-launcher migration/walkthrough tests.
This verdict does not claim full target-system production acceptance.

The user deferred T14 GitLab integration and the T15 40-service qualification.
T16 real GitLab and configured-model qualifications remain unexecuted. Those
limits do not block the requested local authoring and release pilot. Existing
qualification harness files are preparation, not evidence of qualification.

Three private Kotlin repositories exercised service registration, committed
source capture without application builds, bounded recorded evidence reads,
proposal submission and local publication. Fifteen standard service sections
were published and checked CURRENT, with meaning UNASSESSED and 401 explicit
unauthored callable gaps. Source evidence and declared wiring do not establish
deployed behavior. The private source and generated documentation remain local.

A real publication exceeded the existing 64 MiB portable binding limit because
identical evidence was copied into each fragment. Bindings 1.2 now share identical
observations and source records while retaining differing historical versions.
The three-service publication fit in 44,364,806 bytes without raising the limit
or narrowing registered source scopes. Round-trip and missing-record regression
checks passed, as did the existing history/cache-loss test.

The full development gate completed across an initial run and continuation.
The initial run exposed a stale CLI test fixture that lacked the five required
service sections; the fixture was corrected and its focused regression passed
(245.38 s), then all remaining gate commands passed. Earlier successful checks
were not repeated for that fixture-only change. The gate included Rust fmt and
clippy, documentation unit tests, T17 migration/local publication (2), reviewed
process/view cases (2), Kotlin 1.9 Maven documentation, all three Kotlin worker
flow/annotation suites, Java documentation/enrichment and operational recovery,
worker/toolchain regressions, bootstrap (44), skill packaging and usability smoke.

The scoped T15 mutation regression previously passed its 15 cases. No 40-service
qualification was run for this release decision. Release asset builds and
platform packaging remain separate release-workflow results.

The expanded practical walkthrough also registered four domain entities, three
named processes, two proposed Kafka interactions, protected intention notes and
a quantity data-flow view. The view exposed further repeated dependency maps
inside fragment bindings. Bindings 1.3 reconstruct those maps from the exact
retained observations, rejecting missing evidence or simultaneous inline maps.
Round-trip and malformed-input checks passed. The reader now calls an
unassessed saved-process summary a process overview, without implying review.
