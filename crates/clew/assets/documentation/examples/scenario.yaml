schema: codeclew-documentation-process/1.0
id: checkout
title: Checkout and inventory reservation
summary: Validate the order and request an inventory reservation.
root:
  service: orders
  selector:
    language: java
    owner: example.orders.CheckoutController
    name: checkout
    parameterTypes: [example.orders.ReservationRequest]
interactions: [reserve-inventory]
maxDepth: 4
maxNodes: 64
process:
  scope: Order checkout and declared inventory reservation
  participants: [orders, inventory]
  objects: []
  trigger: A checkout request reaches the orders controller.
  outcomes: [Inventory reservation is requested through the declared integration.]
  linkedSubviews: []
